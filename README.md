# kanzi

> Working title. Open-source successor to **CJKV Dict** — a dictionary for the living
> Han-script written systems (Traditional Chinese, Simplified Chinese, Cantonese, Japanese)
> whose headline feature is explaining *why* the same idea is written or read differently
> across them.

This is a clean-room rebuild driven by a design blueprint. The full design document and the
phased implementation plan live in **`docs/`** (kept local, not committed).

## What it is

A free, server-backed online dictionary with three capabilities the original lacks:

1. **A "why it branched" engine** (lexical → orthographic → phonological), built only from
   open datasets — no LLM, no generated prose.
2. **Any-to-any handwriting input** — draw a character your keyboard can't type, in any of
   the four systems, and get candidates shown across all of them.
3. **English-pivot translation search** via a language-independent concept layer.

## Scope

In: Traditional Chinese (TW/HK/Macau), Simplified Chinese (mainland/SG), Cantonese
(first-class, incl. 粵字 + jyutping), Japanese (shinjitai/kyūjitai). Region is a first-class tag.
Out (deliberately): Korean, Vietnamese. Offline use is deferred (see design doc, Appendix A).

## Architecture (locked decisions)

| Layer            | Choice                                                                   |
|------------------|--------------------------------------------------------------------------|
| Backend (serving)| **Rust** — Axum + rusqlite (SQLite/FTS5), single static binary           |
| Data store       | One precompiled **SQLite** file (FTS5), memory-resident at runtime       |
| Build pipeline   | Offline ingestion (Python/JS allowed); **no Python in the serving path** |
| Handwriting      | Proxy the **Google Input Tools** endpoint; PaddleOCR (ONNX via `ort`) fallback |
| Frontend         | **Svelte + Vite** SPA/PWA; `<canvas>` ink → `/recognize`                 |
| Reverse proxy    | nginx serving static frontend, proxying `/api` to the backend binary     |

See `docs/PLAN.md` for the phase-by-phase build plan.

## Layout

```
kanzi/
├── backend/    Rust serving binary (Axum + rusqlite)
├── frontend/   Svelte + Vite SPA/PWA
├── pipeline/   Offline ingestion: open sources → one normalised SQLite DB
├── data/       Built + downloaded artifacts (gitignored, regenerated by pipeline)
└── docs/       Design doc + PLAN.md (gitignored, local working files)
```

## Status

Phase 0 (scaffold). Nothing serving yet — see `docs/PLAN.md`.

## Licensing

The derived dictionary DB will ship under **CC-BY-SA** with an attribution file (most upstream
sources are CC-BY-SA / permissive). GPLv2 classical data (Guangyun/Shuowen/Kangxi via cjkvi-dict)
is kept as a **separate optional module** to keep the core DB's licensing clean.
